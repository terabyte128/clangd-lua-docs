# clangd-lua-docs

This is a dumb plugin that specifically augments documentation for the Lua C
API, when running `clangd`.

## Installation

Lazy:

```lua
{
    'terabyte129/clangd-lua-docs',
    config = true,
}
```
